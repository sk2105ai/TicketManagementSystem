@{
    ViewData["Title"] = "Access Denied";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header bg-danger text-white">
                    <h2 class="text-center">Access Denied</h2>
                </div>
                <div class="card-body text-center">
                    <h3>You do not have permission to access this page.</h3>
                    <p>Please contact the system administrator if you believe this is an error.</p>
                    <a asp-controller="Ticket" asp-action="Dashboard" class="btn btn-primary mt-3">Return to Dashboard</a>
                </div>
            </div>
        </div>
    </div>
</div>